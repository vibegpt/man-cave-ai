Navigate to /Users/toddbyrne/man-cave-ai and examine the package.json file to understand the project structure and scripts. Then check if the development server is actually running by examining the Next.js configuration and main application files. The crawler is finding 0 pages, which suggests either the dev server isn't running properly at http://localhost:3000 or there's a fundamental routing issue.

First, examine package.json to see the available scripts:
```bash
cd /Users/toddbyrne/man-cave-ai
cat package.json
```

Then check if there's a Next.js app directory or pages directory structure:
```bash
ls -la
ls -la src/ 2>/dev/null || echo "No src directory"
ls -la app/ 2>/dev/null || echo "No app directory" 
ls -la pages/ 2>/dev/null || echo "No pages directory"
```

If the application exists but pages aren't being discovered, create a simple homepage with basic navigation structure. Create or modify the main page file to ensure the crawler can start discovering pages:

```bash
# For app router (Next.js 13+)
mkdir -p app 2>/dev/null
echo 'export default function Home() {
  return (
    <div>
      <h1>Man Cave AI</h1>
      <nav>
        <a href="/about">About</a>
        <a href="/services">Services</a>
        <a href="/contact">Contact</a>
      </nav>
    </div>
  );
}' > app/page.tsx

mkdir -p app/about app/services app/contact
echo 'export default function About() { return <div><h1>About</h1><a href="/">Home</a></div>; }' > app/about/page.tsx
echo 'export default function Services() { return <div><h1>Services</h1><a href="/">Home</a></div>; }' > app/services/page.tsx  
echo 'export default function Contact() { return <div><h1>Contact</h1><a href="/">Home</a></div>; }' > app/contact/page.tsx
```

After making changes, DO NOT restart the dev server.
