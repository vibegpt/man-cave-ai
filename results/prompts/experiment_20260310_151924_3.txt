Analyze the website at /Users/toddbyrne/man-cave-ai to identify why the crawl depth is 0/40 and pages crawled is 0. Check if there are navigation issues preventing the crawler from discovering pages.

First, examine the main layout files and homepage to ensure proper internal linking structure exists:

1. Check `src/app/layout.tsx` or similar root layout file for navigation components
2. Examine `src/app/page.tsx` for the homepage structure and internal links
3. Look for any `next.config.js` or routing configuration that might block crawling
4. Check for a sitemap at `public/sitemap.xml` or similar

If the issue is missing navigation, add a basic navigation header with internal links to key pages. If pages don't exist, create placeholder pages with proper internal linking.

Focus on creating a minimal viable navigation structure that allows the crawler to discover pages, specifically targeting the crawl depth score which is currently 0/40 - this is the most critical issue preventing any meaningful internal linking analysis.

Make these changes in the most direct way possible - either fix existing navigation components or add a simple header navigation if none exists. Ensure all internal links use proper Next.js Link components or standard anchor tags with relative URLs.

After making changes, DO NOT restart the dev server.
