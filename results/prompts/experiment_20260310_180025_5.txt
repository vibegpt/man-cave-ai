Navigate to /Users/toddbyrne/man-cave-ai and improve hub connectivity by creating a comprehensive navigation menu with bidirectional links between all pages. First, examine the current page structure by listing all files in the pages or src directory to understand the existing pages. Then create a shared navigation component that includes links to all major sections and pages, ensuring every page has both inbound and outbound internal links. Add this navigation component to the main layout file (likely _app.js, layout.js, or similar) so it appears on every page. Focus on creating a hierarchical navigation structure where hub pages link to child pages AND child pages link back to their parent hubs, plus cross-links between related pages at the same level. The navigation should include proper internal anchor tags with full relative paths (not external URLs) to ensure the crawler can follow all links. Make sure to include the homepage and all 15 other pages in this navigation structure. After making changes, DO NOT restart the dev server.
