Analyze the Next.js application at /Users/toddbyrne/man-cave-ai to identify why 0 pages are being crawled. Check if there's a robots.txt file blocking crawlers, examine the sitemap.xml generation, and verify the homepage has proper internal links.

First, check if robots.txt exists and is blocking crawlers:
```
cat /Users/toddbyrne/man-cave-ai/public/robots.txt
```

If it blocks crawlers, modify it to:
```
User-agent: *
Allow: /
```

Next, examine the homepage component to ensure it has visible internal links. Look for the main page component (likely pages/index.js or app/page.js) and add a simple navigation menu with links to key sections:

```javascript
<nav>
  <ul>
    <li><a href="/">Home</a></li>
    <li><a href="/about">About</a></li>
    <li><a href="/services">Services</a></li>
    <li><a href="/contact">Contact</a></li>
  </ul>
</nav>
```

Create missing pages referenced in navigation if they don't exist. Ensure all pages have proper HTML anchor tags (not just Next.js Link components) so crawlers can follow them.

Check if there's a sitemap.xml in the public folder and create one if missing:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>http://localhost:3000/</loc></url>
  <url><loc>http://localhost:3000/about</loc></url>
  <url><loc>http://localhost:3000/services</loc></url>
  <url><loc>http://localhost:3000/contact</loc></url>
</urlset>
```

The goal is to make the homepage discoverable and provide clear navigation paths to other pages so the crawler can find and index content.

After making changes, DO NOT restart the dev server.
