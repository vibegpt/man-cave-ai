Navigate to /Users/toddbyrne/man-cave-ai and first examine the current file structure to understand the application layout. Then check if there's a sitemap.xml file in the public directory, and if not, create one that includes all discoverable pages with proper internal linking structure.

Look at the pages directory structure and identify all available routes. Create a comprehensive sitemap.xml file in the public directory that lists all pages including the homepage, any dynamic routes, and static pages. This will help the crawler discover pages that may not be linked internally.

Examine these key files:
- Check app/page.tsx or pages/index.tsx for the homepage
- Look for any dynamic routes in [brackets]
- Check for static pages like about, contact, etc.
- Examine any existing navigation components for route discovery

Create public/sitemap.xml with proper XML structure including:
```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>http://localhost:3000/</loc></url>
  <!-- Add all discovered routes here -->
</urlset>
```

The goal is to provide the crawler with a complete map of all available pages since the current crawl depth of 0/40 suggests pages aren't being discovered through internal navigation alone.

After making changes, DO NOT restart the dev server.
