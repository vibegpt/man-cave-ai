Navigate to /Users/toddbyrne/man-cave-ai and examine the package.json to understand the application structure. The critical issue is that 0 pages are being crawled, resulting in a 0/40 crawl depth score. Check if the Next.js application is properly configured and running on localhost:3000.

First, examine the package.json file to understand the project setup:

```bash
cd /Users/toddbyrne/man-cave-ai
cat package.json
```

Then check if there's a pages directory or app directory structure:

```bash
ls -la
find . -name "*.js" -o -name "*.jsx" -o -name "*.ts" -o -name "*.tsx" | head -20
```

Examine the main application files to understand the routing structure:

```bash
# Check for Next.js pages
if [ -d "pages" ]; then ls -la pages/; fi
if [ -d "app" ]; then ls -la app/; fi
if [ -d "src/pages" ]; then ls -la src/pages/; fi
if [ -d "src/app" ]; then ls -la src/app/; fi
```

Create a simple index page if none exists to ensure the crawler has something to discover:

```bash
mkdir -p pages
echo 'export default function Home() {
  return (
    <div>
      <h1>Man Cave AI</h1>
      <p>Welcome to Man Cave AI</p>
    </div>
  );
}' > pages/index.js
```

After making changes, DO NOT restart the dev server.
