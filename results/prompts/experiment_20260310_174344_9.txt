Navigate to /Users/toddbyrne/man-cave-ai and examine the project structure to understand why the crawler finds 0 pages. First, check if the development server is running by examining package.json and any running processes. Then examine the Next.js routing structure in the app or pages directory to identify existing pages. Look for any issues preventing the crawler from discovering pages at http://localhost:3000 - this could be due to missing pages, routing configuration issues, or the dev server not being accessible. Create a simple index page at the root route if none exists, ensuring it contains basic HTML structure and at least one internal link to make it crawlable. The goal is to get the crawler to discover at least one page so we can begin measuring actual internal linking metrics instead of getting 0 pages crawled.

After making changes, DO NOT restart the dev server.
