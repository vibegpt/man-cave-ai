Navigate to /Users/toddbyrne/man-cave-ai and examine the application structure to understand why 0 pages are being crawled. First, check if the development server is running by examining the package.json scripts and looking for any routing configuration. Then examine the app directory structure to identify all pages and routes.

If pages exist but aren't discoverable, create a comprehensive sitemap.xml file in the public directory that lists all available routes with proper XML structure. Include the homepage and any dynamic routes found in the app directory.

Additionally, ensure there's a robots.txt file in the public directory that doesn't block crawling of the site content.

If the issue is that pages exist but have no internal links connecting them, add a navigation component that links to all major sections of the site. Place this navigation in the main layout file to ensure it appears on every page.

Focus specifically on making the homepage discoverable and ensuring it contains links to all other pages in the application. Check if there are any Next.js specific routing files like page.js, layout.js, or route.js that need to be examined.

The goal is to fix the fundamental issue where 0 pages are being discovered by the crawler, which is preventing any meaningful internal linking analysis.

After making changes, DO NOT restart the dev server.
