Navigate to /Users/toddbyrne/man-cave-ai and improve hub connectivity (currently 5/10) by implementing a strategic hub-to-hub cross-linking system. First, examine the current site structure by running `find . -name "*.tsx" -o -name "*.js" -o -name "*.md" | head -20` to understand the page structure. Then locate the main layout or navigation component and add bidirectional cross-references between hub pages. 

Create a new component `components/HubNavigation.tsx` that generates contextual links between related hubs. In this component, implement a hub-to-hub linking matrix where each hub page includes 3-4 strategic links to other relevant hubs, ensuring bidirectional connectivity.

Next, modify the main layout file (likely `app/layout.tsx` or similar) to include this hub navigation component on all hub-type pages. The links should be contextually relevant - for example, if there are service hubs, location hubs, or category hubs, ensure they cross-reference each other logically.

Finally, update any existing hub pages to include inline contextual links to 2-3 other hub pages within the content body, not just in navigation. This creates the bidirectional hub<->child relationship that the ILS hub_connectivity metric specifically measures.

Focus on creating meaningful hub-to-hub connections that would naturally exist in a well-structured site architecture, avoiding generic footer links that previous experiments may have found ineffective.

After making changes, DO NOT restart the dev server.
