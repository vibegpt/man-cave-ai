Navigate to /Users/toddbyrne/man-cave-ai and examine the application structure to identify why the crawler finds 0 pages. First, check if the dev server is running on port 3000 by examining the package.json and any running processes. Then inspect the main page files to understand the routing structure.

Look for the homepage file (likely pages/index.js, app/page.js, or similar) and examine its content. The critical issue is that the crawler cannot discover any pages, which suggests either:
1. The dev server isn't properly serving content
2. The homepage has no discoverable links
3. There's a routing or build issue

Once you identify the homepage file, add a simple navigation menu with internal links to create discoverable pages. Create at least 3-5 basic pages (like /about, /services, /contact, /locations) and link them from the homepage using standard HTML anchor tags or Next.js Link components.

If pages don't exist, create them as basic functional components. The goal is to establish a foundational site structure that the crawler can discover and traverse, moving from 0 crawled pages to a functional site with interconnected pages.

Focus on creating the most basic navigable structure first - without any pages being crawled, all other ILS metrics are meaningless.

After making changes, DO NOT restart the dev server.
