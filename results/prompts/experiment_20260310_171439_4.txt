Analyze the Next.js application at /Users/toddbyrne/man-cave-ai to identify why the crawler cannot find any pages. The fundamental issue is 0 pages crawled, indicating the homepage at http://localhost:3000 is either not accessible or has no discoverable internal links.

First, check if the development server is running by examining the package.json for the dev script and any running processes. Then inspect the homepage route file (likely app/page.tsx or pages/index.tsx) to ensure it contains valid HTML with internal navigation links.

If the homepage exists but lacks internal links, add a simple navigation structure. Create or modify the main layout component to include a basic navigation menu with links to key site sections. Focus on creating discoverable internal links that will allow the crawler to find and traverse multiple pages.

Check these files in order:
1. /Users/toddbyrne/man-cave-ai/package.json - verify dev script
2. /Users/toddbyrne/man-cave-ai/app/page.tsx or /Users/toddbyrne/man-cave-ai/pages/index.tsx - homepage
3. /Users/toddbyrne/man-cave-ai/app/layout.tsx or /Users/toddbyrne/man-cave-ai/components/layout.tsx - main layout

If pages exist but are not linked, add a navigation component with internal links. If no pages exist beyond homepage, create at least 2-3 basic pages (about, services, contact) and link them from the homepage navigation.

The goal is to make the site crawlable by ensuring the homepage has discoverable internal links to other pages within the same domain.

After making changes, DO NOT restart the dev server.
