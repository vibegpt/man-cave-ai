Analyze the internal linking structure of the website at /Users/toddbyrne/man-cave-ai and add bidirectional cross-links between related state hub pages to improve hub connectivity score.

First, examine the current state hub pages by running:
```
find /Users/toddbyrne/man-cave-ai -name "*.tsx" -o -name "*.jsx" -o -name "*.js" -o -name "*.ts" | grep -E "(state|hub)" | head -10
```

Then examine the content of 3-4 state hub page files to understand their current structure and identify where to add cross-references to other related state pages.

Create a "Related States" or "Nearby Locations" section in each state hub page that links to 3-4 geographically adjacent or similar states. Focus on creating reciprocal links - if State A links to State B, ensure State B also links back to State A.

For example, if you find pages like california.tsx, nevada.tsx, arizona.tsx, add a section at the bottom of each page before the footer:

```typescript
<section className="related-states">
  <h3>Explore Nearby States</h3>
  <div className="state-links">
    <Link href="/california">California</Link>
    <Link href="/nevada">Nevada</Link>
    <Link href="/arizona">Arizona</Link>
  </div>
</section>
```

Ensure the links use proper relative paths and match the existing routing structure. Make the styling consistent with the existing design system.

Target adding cross-links between at least 8-10 state hub pages, creating a web of bidirectional connections that will increase the hub connectivity score from 5/10 toward 8-10/10.

After making changes, DO NOT restart the dev server.
