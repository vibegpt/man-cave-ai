Analyze the Next.js application at /Users/toddbyrne/man-cave-ai to identify why the crawler is finding 0 pages and fix the root cause. First, check if the development server is running on http://localhost:3000 by examining the package.json scripts and any server configuration files. Then check the pages directory structure and routing setup.

If the server isn't running, start it by running `npm run dev` or `yarn dev` in the project directory. If pages exist but aren't being discovered, check for:

1. Missing or incorrect robots.txt that blocks crawling
2. No-index meta tags preventing page discovery  
3. Missing sitemap.xml file
4. Broken internal navigation structure

Focus on the most basic issue first: ensure there's a functional homepage at / that can be crawled, then verify there are actual pages with internal links between them. Check app/page.tsx or pages/index.tsx for the homepage, and look for any Link components or anchor tags that create internal navigation.

If no pages directory exists, create a basic page structure with app/page.tsx (homepage), app/about/page.tsx, and app/contact/page.tsx, ensuring each page includes Next.js Link components pointing to the other pages to establish basic internal linking.

After making changes, DO NOT restart the dev server.
