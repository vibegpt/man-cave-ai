Navigate to /Users/toddbyrne/man-cave-ai and improve PageRank distribution (currently 19.0/20, Gini=0.05) by creating a product recommendation carousel that cross-links content pages through shared affiliate products.

First, examine the current product mappings:
```bash
cat src/data/productMappings.ts
```

Then examine a few content pages to understand the current relatedPages structure:
```bash
head -50 src/app/man-cave-bar/page.tsx
head -50 src/app/basement-man-cave-ideas/page.tsx
```

Now create a new component that will inject product-based cross-links. Create the file src/components/ProductCrossLinks.tsx:
```typescript
import Link from 'next/link';

interface ProductCrossLinksProps {
  currentPage: string;
  productCategories: string[];
}

export default function ProductCrossLinks({ currentPage, productCategories }: ProductCrossLinksProps) {
  // Map product categories to related content pages
  const categoryPageMap: Record<string, string[]> = {
    'lighting': ['/man-cave-lighting', '/basement-man-cave-ideas'],
    'furniture': ['/man-cave-furniture', '/man-cave-bar'],
    'decor': ['/man-cave-decor', '/man-cave-wall-decor', '/man-cave-signs'],
    'storage': ['/man-cave-storage', '/garage-man-cave'],
    'entertainment': ['/golf-simulator-man-cave', '/basement-man-cave-ideas']
  };

  const relatedPages = productCategories
    .flatMap(category => categoryPageMap[category] || [])
    .filter((page, index, arr) => arr.indexOf(page) === index && page !== currentPage)
    .slice(0, 3);

  if (relatedPages.length === 0) return null;

  return (
    <div className="mt-8 p-4 bg-gray-50 rounded-lg">
      <h3 className="text-lg font-semibold mb-3">Shop Related Man Cave Ideas</h3>
      <div className="flex flex-wrap gap-2">
        {relatedPages.map(page => (
          <Link key={page} href={page} className="px-3 py-1 bg-blue-600 text-white rounded text-sm hover:bg-blue-700">
            {page.replace('/man-cave-', '
